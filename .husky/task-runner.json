{
   "$schema": "https://alirezanet.github.io/Husky.Net/schema.json",
   "tasks": [
      {
         "name": "build-check",
         "command": "dotnet",
         "args": [ "build", "--no-restore", "--verbosity", "quiet" ],
         "include": ["*.sln"],
         "continueOnError": false
      },
      {
         "name": "auto-format",
         "command": "dotnet",
         "args": [ "format", "--verbosity", "minimal", "--exclude", "**/bin/**", "--exclude", "**/obj/**" ],
         "include": ["*.sln"],
         "continueOnError": false
      }
   ]
}
